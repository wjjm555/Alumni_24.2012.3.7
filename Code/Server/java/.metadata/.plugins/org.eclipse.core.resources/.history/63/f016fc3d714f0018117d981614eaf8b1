package com.gbs.app.model.skills;

import com.gbs.app.model.Base;
import com.gbs.app.model.players.Player;

public abstract class Skill extends Base {

	private final Type type;
	private final Mode mode;

	public Skill(Type type) {
		this.type = type;
		this.mode = mode;
	}

	public enum Mode {

	}

	public enum Type {
		Common(""), Master(""), Awaken(""), Lock("");
		private final String name;

		private Type(String name) {
			this.name = name;
		}

		public String getName() {
			return name;
		}
	}

	public Type getType() {
		return type;
	}

	public abstract Object execute(Player... roles);

	public abstract Object judge(Player... roles);
}
